# 项目全局要求

## 修改原则

- 保持逻辑简洁、健壮、易维护，优先解决根因，避免针对单一现象堆叠补丁。
- 关注低资源生产环境的性能，优先复用现有边界和数据流；没有明确收益时，不增加依赖、抽象或兜底分支。
- 新增外部站点适配时，按数据协议实现通用逻辑，不为某个站点的当前返回内容硬编码结果。
- 不删除项目文件；确需清理生成物或缓存时，在交付说明中列出并由用户处理。

## 发布顺序

- 每次修改完成后，先运行相关测试、完整测试和静态检查，再检查差异与敏感信息。
- 不提交 `.env`、密码、API Key、令牌、Cookie 或其他凭据；凭据只通过环境变量或服务器受保护配置提供。
- 必须先提交并成功推送 GitHub，确认远端包含待发布提交后，才能部署生产服务器。
- 生产部署前保留可恢复的程序与数据库备份；部署后检查服务状态、健康接口和实际页面。
